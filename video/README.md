# Kevrai Omni rolling video

This folder contains an isolated Remotion project. It is not bundled into the Electron application.

## Commands

```bash
npm install
npm run studio
npm run render
```

The rolling CI workflow renders `KevraiDemo`, injects current catalog counts, and updates the `rolling-demo` GitHub Release asset.
