{
  "name": "kevrai-omni",
  "productName": "Kevrai Omni",
  "version": "2.7.0",
  "description": "Kevrai Omni — one-click local AI workstation (LLM/TTS/Image/Video/3D/Audio/Super-Resolution). MiniMax-Music3 music generation, LTX-2.5 video, super search, llama.cpp + MNN dual engine, hardware-aware recommendation.",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "cross-env ELECTRON_ENABLE_LOGGING=1 electron . --enable-logging --devtools",
    "test:python": "cd python && python -m pytest -q tests/",
    "test:js": "node --check electron/main.js && node --check electron/preload.js && node --check renderer/app.js && node --check renderer/modules/api.js && node --check renderer/modules/models.js && node --check renderer/modules/search.js && node --check renderer/modules/ltx.js && node --check renderer/modules/engines.js && node --check renderer/modules/onboarding.js && node --check renderer/modules/downloads.js && node --check renderer/bootstrap.js",
    "smoke": "node --check electron/main.js && node --check electron/preload.js && node --check renderer/app.js",
    "test:smoke": "bash scripts/smoke.sh",
    "build:win": "electron-builder --win --x64",
    "build:linux": "electron-builder --linux",
    "build:mac": "electron-builder --mac",
    "release": "bash scripts/release.sh"
  },
  "author": {
    "name": "Kevrai Omni contributors",
    "email": "kevrai@local"
  },
  "license": "SEE LICENSE IN LICENSE",
  "homepage": "https://github.com/Bullobis/kevrai-omni",
  "repository": {
    "type": "git",
    "url": "https://github.com/Bullobis/kevrai-omni.git"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  },
  "dependencies": {}
}
