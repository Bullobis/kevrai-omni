# Kevrai Omni v3.2.0 — 最终整合：GGUF 性能 + 命令面板/主题/数据目录

发布日期：2026-09-25　·　上一版本：v3.1.0

v3.2.0 是在 v3.1.0（后端可靠性 + MNN 子进程隔离）之上的**最终整合版本**，
叠加 GGUF 枚举性能优化与渲染层易用性增强，无破坏性变更。

## 主要变更

### 1. GGUF 仓库枚举性能优化
- `/api/gguf-repos` 各仓库由串行改为线程池**并发**枚举，并增加 10 分钟 TTL 缓存
  （`?force=true` 绕过）；缓存命中约 2ms。
- GGUF 枚举移出前端首屏关键路径：后台加载、就绪后单独渲染，不再阻塞模型市场首绘。
- 从枚举 API 镜像列表移除持续 403 的 `hf-cdn.sufy.com`，避免每次先等待 403 往返
  （运行时硬阻断与目录零引用保持）。

### 2. 渲染层易用性（命令面板 / 主题 / 数据目录）
- 命令面板（Ctrl/⌘+K）：修复浮层常驻与占位符未翻译两处真实缺陷。
- 主题：浅色/深色切换，首绘前同步初始化，消除首启动主题闪烁（FOUC）。
- 数据目录可配置；侧边栏可折叠；最近使用排序。

### 3. 延续 v3.1.0 的后端可靠性
- MNN 引擎独立子进程隔离（opt-in，`KEVRAI_MNN_SUBPROCESS=1`）。
- 跨模块资源泄漏修复、下载断点续传与跨镜像回退、统一 JSON 异常处理。

## 质量门（真实数字，以最终整合分支实跑为准）
- Python 全量测试：**1078 passed / 0 failed**（最终整合分支实跑）。
- JavaScript 语法检查：28/28（node --check，实跑）。
- Linux AppImage 真实冒烟（Xvfb + CDP）：sidecar v3.2.0、首屏模型网格渲染、
  GGUF 后台加载、浅色主题正确，零阻塞。

## 下载
- Linux：`Kevrai-Omni-3.2.0-x86_64.AppImage`、`Kevrai-Omni-3.2.0-amd64.deb`
- 下载后用随附 `SHA256SUMS.txt` 校验：`sha256sum -c SHA256SUMS.txt`
- Windows / macOS 产物以对应平台 CI 构建的 Release 资产为准。

## 自运行（不依赖任何会话）
- 每日自维护巡检、每周安全审计、每周宣传视频重渲染、每月短片渲染均由
  GitHub Actions 自动执行；接续入口见记忆仓库 `CLUSTER_RUNBOOK.md`。
