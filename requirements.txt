# H3Studio 运行依赖（仅直接依赖；diffsynth 会自带 transformers/safetensors 等传递依赖）
# 全部为 PyPI 真实存在的包，2026-08-08 核对

# ── 界面（LGPL 协议，商用分发友好）──
PySide6>=6.7

# ── 推理引擎（DiffSynth-Studio 2.1.0+ 才含 MiniMaxH3Pipeline）──
diffsynth>=2.1.0

# ── 音视频处理（生成结果写出 / 参考素材读取 / 缩略图）──
av>=12.0                # PyAV：视频音频解码
torchaudio              # 音频重采样（需与 torch 版本匹配）
imageio>=2.31           # 视频首帧缩略图
imageio-ffmpeg          # imageio 的 ffmpeg 后端
pillow>=10.0

# ── NF4 量化支持（0.43+ 官方支持 Windows 11；ROCm 亦支持）──
bitsandbytes>=0.43

# ── 下载与工具 ──
requests>=2.31          # 自研下载器 / 测速
psutil>=5.9             # 内存检测
numpy

# 注意：torch 需按硬件单独安装（脚本已自动处理，手动安装参考）：
#   NVIDIA: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#           （RTX 20~50 系；GTX 10/900 系老卡换 cu126；切勿用国内 PyPI 镜像装 torch，
#            镜像默认匹配 CPU 版 —— 2026-08 实测确认）
#   AMD Linux: pip install torch torchaudio --index-url https://download.pytorch.org/whl/rocm6.2
#   华为昇腾: pip install torch==2.7.1 torch-npu==2.7.1（DiffSynth 官方 npu 依赖组合）
# Python 版本要求：3.10 ~ 3.14（推荐 3.12/3.13；3.15 暂无 Windows 版 PyTorch）
