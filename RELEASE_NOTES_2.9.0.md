# Kevrai Omni v2.9.0 — 视觉全局重构 + 短剧并入 Agent + Skill Hub + CI 可信度修复

发布日期：2026-09-23
上一版本：v2.8.1

本次为**功能与工程质量并重的版本**，无破坏性变更。三条主线：界面按 Cherry Studio
的设计令牌体系全局重构；短剧能力并入 Agent 并引入本地技能库；以及一批
「CI 该红却绿」的缺陷修复。

---

## 1. 界面全局重构（对齐 Cherry Studio 令牌体系）

| 项 | 变更 |
|---|---|
| 表面层级 | 引入 `--stack-0..3` 四层表面令牌，**层级越高越亮**，与 Cherry Studio 完全一致；移除全部毛玻璃/模糊/渐变 |
| 圆角与字重 | 统一圆角、字号、字重阶；侧栏改为 64px 图标轨 |
| 浅色主题可读性 | 修复 Agent 消息区对比度仅 **1.15:1**（几乎不可读，低于 WCAG AA 的 4.5:1）→ 提升至 **14.74:1**；两套主题实测均 ≥ 5.98:1 |

## 2. 短剧 Agent 并入 Agent + 本地技能库（Skill Hub）

- **前端 tab 移除，完全并入 Agent**。后端此前已是合并状态：`DRAMA_TOOLS` 早已
  封装为内置技能 `drama_studio`，`python/app/drama.py` 与全部 6 条
  `/api/drama/*` 路由**逐字节未改动**。
- 新增本地技能库，支持三种真实来源：目录 / zip / git。
- 外部技能为**引导性**（`tools=[]`），不获得工具执行权。
- **原有技能一律不删**，在四层上强制：`extra_skills` 绕过（永不修改
  `BUILTIN_SKILLS`）、三条导入路径均拒绝内置名、删除接口对内置技能返回
  HTTP 403（6 个内置逐一实测）。
- 内置技能集确认仍为：`core`、`model_catalog`、`local_system`、`drama_studio`、
  `writing_studio`、`media_prompt_studio`。

## 3. CI 可信度修复（本版最重要的工程质量改动）

### 3.1 mypy 门槛原本可被完全绕过

原写法 `mypy ... || true` 后数 `grep -c "error:"`，而 mypy 只要没跑到逐行诊断
（缺依赖、配置写坏、路径错、解释器不匹配）就只输出一条 usage 消息，**不含
`error:` 行** → 计数为 0 → 门槛判定「未倒退」直接放行。**该红的时候会绿。**

已改为先取 mypy 真实退出码，`>=2`（无法运行）判硬失败。三向验证：干净仓库
通过、注入 1 个类型错误失败、`app/` 不存在时失败（旧版本此处会绿）。

### 3.2 类型欠账清零

mypy 错误数 **29 → 0**，棘轮从「零余量」改为「零容忍」。此前 `MAX_ERRORS=29`
而实测恰好 29，任何一个无关模块的新错误都会让 CI 变红。改动全部为类型标注与
收窄，不改运行时行为。

### 3.3 其余 CI 加固

- 覆盖率门槛：区分「测得很差」与「压根没测」，避免误报为 floor 未达标而查错方向
- `smoke.sh` 的 pip 步骤：由裸 `|| true` 改为显式记录成败，失败不再静默
- 覆盖率门槛 65% → 68%（实测 69.6%），`docs/COVERAGE.md` 按新基线重写

### 3.4 测试与覆盖率

- 用例数 **579 → 836**；`recommend.py` 覆盖率 **7% → 100%**
- 删除死代码 `runner.py`（零调用者）
- 修复 `test_json_integrity.py` 的缓存污染：本地跑过 mypy 会让用例数从 836
  虚增到约 2 万（`.mypy_cache` 下上万 JSON 被当作待校验文件），造出
  「本地比 CI 查得多」的假象

## 4. 其他缺陷修复

| 项 | 说明 |
|---|---|
| 版本停滞 | 根因是测试硬编码 `== "2.8.1"` **反向锁死**版本；改为关系式断言，7 处版本号统一升至 2.9.0 |
| `escapeHtml` 重复 7 份 | 其中 `downloads.js` 那份使用裸 `String(s)`，会把 `null`/`undefined` 当字面量渲染进下载源列表；已收敛为 `net.js` 单份实现 |
| 推荐排序符号反转 | 分数相同时**大模型排在前面**，与模块自身文档相反；已修正并加测试钉住 |
| 下载连接池 | `_own_client` 未在 `__init__` 声明，靠 `getattr/hasattr` 探测；已规整并补 4 条生命周期测试 |

## 5. 包内容与签名

- Windows：NSIS 安装包 + 便携 zip；均经 Authenticode 签名（含时间戳），
  `latest.yml` 的 `size`/`sha512` 与签名后镜像一致
- **差分更新已启用**：blockmap 在**签名后**重建，并校验平铺不变式
  （`sum(sizes) == 安装包字节数`）。签名前生成的 blockmap 对签名后镜像必然失效，
  这正是本版专门验证的顺序问题

## 6. 兼容性

无破坏性变更。配置、技能、目录格式向后兼容。原有技能不会被删除或改名。
