# Kevrai Omni v3.1.0 — 后端可靠性加固 + MNN 子进程隔离

发布日期：2026-09-25　·　上一版本：v3.0.0

本版本为增量增强，重点在后端（Python sidecar）的可靠性、资源安全与崩溃隔离；前端视觉与打包发布工程沿用 v3.0.0 成果。**未引入任何新的第三方依赖**（全部使用标准库与既有依赖）。

## 主要变更

### 1. MNN 引擎独立子进程隔离（opt-in）
- 设置环境变量 `KEVRAI_MNN_SUBPROCESS=1` 启用；默认关闭，进程内路径保持不变。
- C++ 引擎发生硬死锁时，父进程按墙钟超时 `SIGKILL` 子进程，由操作系统回收全部资源，下次加载自动重建。
- 子进程统一采用 `spawn` 启动，消除多线程环境下 `fork` 导致的死锁风险，行为与 Windows 一致。
- 含空闲崩溃看门狗、异常类型跨进程还原与干净关闭。

### 2. 跨模块资源泄漏修复
- 应用关闭（lifespan）时正确关闭 hub 注册表单例。
- 下载并发设置变更后，旧连接池在途请求排空再关闭。
- 多模态对话产生的临时媒体，在成功 / 失败 / 取消 / SSE 断连的全部路径下清理。
- 引擎 `download.tmp` 在失败时清理；hub jobs 进度字段修正。

### 3. 既有可靠性增强（部分随 v3.0.0 已落地，本版本延续）
- Agent：ReAct 防御、中文最终答案、工具调用容错、工具重复注册告警、整数入参安全转换。
- 引擎安装全局互斥；托管 Python 环境升级修复与中文报错。
- 视频生成支持取消、GIF 回退与历史记录封顶。
- 下载器断点续传退避与跨镜像中途回退、SHA256 完整性兜底。
- 设置写入强校验；全局统一 JSON 异常处理；搜索结果按版本缓存；清除失效钓鱼镜像。

### 4. GGUF 仓库枚举性能优化
- `/api/gguf-repos` 各仓库由串行改为线程池**并发**枚举，并增加 10 分钟 TTL 缓存（`?force=true` 可绕过）；缓存命中约 2ms。
- GGUF 枚举移出前端首屏关键路径：后台加载、就绪后单独渲染，不再阻塞模型市场首绘，加载期显示“加载中”。
- 从枚举 API 镜像列表移除持续 403 的 `hf-cdn.sufy.com`，避免每次先等待一次 403 往返（运行时硬阻断与目录零引用保持不变）。

## 质量门（真实数字）
- Python 全量测试：**1078 passed / 0 failed**（相对最初基线 833 累计净增 245，以最终整合分支实跑为准）。
- JavaScript 语法检查：28/28 通过（node --check，以实跑为准）。
- 真实 sidecar 集成冒烟（子进程启动 / 8 个端点 / 关闭）：通过，零孤儿进程。
- 50 轮资源浸泡测试：文件描述符 / 线程 / 子进程数量零增长。
- 25 轮 MNN 子进程 kill/respawn 高压：文件描述符恒定、无僵尸 / 孤儿进程。
- catalog：121 个模型 / 32 个引擎，JSON 与 jsonschema 校验无错误。
- 密钥 / token 扫描：无命中，零凭证入库。

## 下载与校验
- Linux：`Kevrai-Omni-3.1.0-x86_64.AppImage`、`Kevrai-Omni-3.1.0-amd64.deb`
- Windows：便携包 `Kevrai-Omni-3.1.0-x64.zip`（NSIS 安装器视构建环境补充）
- 下载后可用随附的 `SHA256SUMS.txt` 校验完整性：`sha256sum -c SHA256SUMS.txt --ignore-metadata`

## 已知验证缺口（如实说明）
- 无头构建环境无法实跑 Electron 图形界面、真实下载模型 / 引擎，以及在真实 GPU/NPU 上推理。
- MNN 子进程模式在真实 MNN C++ 引擎上的端到端行为需在具备 GPU 的环境进一步验证（协议、超时、崩溃重建、资源有界性已用模拟对象验证）。
- Windows / macOS 路径以代码审查与跨平台逻辑为主，未在对应操作系统实跑；macOS dmg 需在 macOS 机构建。
